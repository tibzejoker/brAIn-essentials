{
  "name": "brain",
  "description": "Central consciousness node. Inspects the network, manages node lifecycle, rewires subscriptions, reflects, and orchestrates the entire brAIn network. Has root authority.",
  "tags": ["consciousness", "orchestrator", "llm", "root"],
  "default_authority": 2,
  "default_priority": 10,
  "ports": {
    "inputs": {
      "user_message": {
        "description": "Free-form text from the user. Anything published on a topic bound here gets the brain's attention.",
        "inputSchema": {
          "type": "object",
          "properties": { "content": { "type": "string" } },
          "required": ["content"]
        }
      },
      "addressed_message": {
        "description": "Anything addressed to the central consciousness — questions, instructions, status updates from other nodes.",
        "inputSchema": { "type": "object" }
      },
      "alert": {
        "description": "High-priority alert from any node that needs the brain's attention or intervention.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "title": { "type": "string" },
            "description": { "type": "string" },
            "source_context": { "type": "string" },
            "suggested_action": { "type": "string" },
            "requires_ack": { "type": "boolean" }
          },
          "required": ["title", "description"]
        }
      },
      "reset_signal": {
        "description": "Wipe the brain's short-term context (conversation buffer + iteration count) so the next turn starts fresh. No payload required.",
        "inputSchema": { "type": "object" }
      }
    },
    "outputs": {
      "brain_response": {
        "description": "Reply from the brain. Topic-bound to chat.response by default but the name is generic — the same port lets the brain talk to any sink (TTS, log, web)."
      }
    }
  },
  "default_port_bindings": {
    "inputs": {
      "user_message": ["chat.input"],
      "addressed_message": ["brain.*"],
      "alert": ["alerts.*"],
      "reset_signal": ["chat.reset"]
    },
    "outputs": {
      "brain_response": ["chat.response"]
    }
  },
  "default_subscriptions": [],
  "default_publishes": ["chat.response"],
  "has_ui": true,
  "supports_transport": ["process"]
}
